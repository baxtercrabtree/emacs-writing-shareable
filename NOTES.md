# NOTES

This file is a lightweight maintainer handoff for future chats. It is meant to
capture active cleanup targets and open questions, not project history or a
full roadmap.

## Next Session

- tighten README wording after recent upstream edits so the tone stays conversational but rigorous
- decide whether to keep or simplify migration and adoption language in `README.md`
- consider rewording the `early-init.el` description in `README.md` to make it more user-facing
- resolve whether `scroll-step` belongs in `lisp/core/core-defaults.el` or should move to a more specific layer
- decide whether `MIGRATION.md` should stay separate, be renamed, or be folded into `README.md`
- revisit whether `ido` remains the right default completion choice
