# Shareable Emacs Config

This is a small, shareable Emacs configuration with optional modules and local overrides. It is designed to stay readable, modular, and easy to adapt to different writing-focused setups.

## Goals

- provide a clean starter config that can be evaluated without committing to a full migration
- bootstrap packages instead of committing `elpa/`
- separate reusable config from machine-specific state
- keep the Org writing workflow available as an optional module

## Layout

- `init.el`: thin entrypoint that loads the rest of the config in order
- `early-init.el`: startup defaults that must run before normal init
- `lisp/core/`: shared layers for package bootstrap, settings, defaults, UI, editing, and module loading
- `lisp/modules/`: optional features such as theme support, core Org support, and writing extras
- `local/init.el`: untracked local overrides for paths, fonts, and enabled modules
- `local/custom.el`: untracked Custom UI state generated by Emacs
- `var/`: untracked runtime state for backups and autosaves

## How It Fits Together

The startup model is intentionally simple:

1. `init.el` establishes the config root and updates `load-path`.
2. `lisp/core/core-bootstrap.el` ensures package management and `use-package` are ready.
3. `lisp/core/core-settings.el` defines the user-facing variables and loads `local/init.el` if present.
4. The remaining `lisp/core/` files apply shared behavior for UI, editing, and defaults.
5. `lisp/core/core-modules.el` loads only the optional modules named in `ews-enabled-modules`.

This keeps the shared repo readable while still allowing machine-specific customization in `local/init.el`.

## Setup

1. Download or clone this repository.
2. Copy `local/init.example.el` to `local/init.el`.
3. Edit `local/init.el` to set your paths, fonts, enabled modules, and any optional settings you want to use.
4. Start Emacs with this repository as the init directory:

```sh
emacs --init-directory=/path/to/emacs-writing-shareable
```

If you want to make it your default config, place it where Emacs loads your normal init directory or create a symlink to it.

## Optional Modules

Modules are controlled with `ews-enabled-modules` in `local/init.el`.

- `theme-modus`: Modus themes plus optional circadian switching
- `org-base`: core Org defaults, agenda/capture bindings, Org file locations, and Markdown export
- `org-writing`: writing-focused extras on top of `org-base`, including journal support, `olivetti`, and `wc-mode`

If you enable `org-writing`, it automatically loads `org-base` first. Use `org-base` alone if you want Org support without the writing-specific presentation tweaks.

## Architecture Guidelines

Use these boundaries when deciding where a change belongs:

- `lisp/core/`: startup-wide behavior that should apply regardless of which optional modules are enabled
- `lisp/modules/`: opt-in features or workflows that can be enabled, disabled, or split independently
- `local/init.el`: machine-specific paths, fonts, local preferences, and toggles that should not be committed
- ignored runtime state: generated files such as `elpa/`, `var/`, `local/custom.el`, autosaves, and other editor state that should stay out of git

A good rule of thumb is:

- if a setting should apply to almost every use of this config, put it in `lisp/core/`
- if a setting supports one workflow such as writing or theme behavior, put it in `lisp/modules/`
- if a setting contains machine-specific knowledge, keep it in `local/init.el`
- if a file is generated by Emacs while running, keep it ignored rather than designing around it

## Naming Scheme

The shared namespace now uses `ews-`, short for "Emacs Writing Shareable."

- use `ews-` for user-facing variables, commands, and setup functions
- use `ews--` for internal helpers that are not intended as part of the public config surface
- prefer names like `ews-org-writing-setup` for functions added to hooks, rather than naming a function `...-hook`

This prefix is intentionally generic and repository-scoped. If you want to rename the namespace in a fork, feel free to do that. The important part is to keep one consistent prefix so your functions and variables are easy to identify and unlikely to collide with package code.

## Product Decisions

Use this section to record decisions that affect the public shape of the config but are not really architectural rules.

- completion stack: `ido` remains the current default because it satisfies the need to find and open files quickly without relying on Dired or buffer navigation
- completion stack follow-up: `ido` is still a future replacement candidate because it is older than the newer Vertico/Orderless-style completion stack

## Local Settings

Configure these in `local/init.el` as needed:

- `ews-enabled-modules`
- `ews-org-directory`
- `ews-org-journal-directory`
- `ews-default-fixed-font`
- `ews-default-variable-font`
- `ews-ispell-program`
- `ews-enable-circadian`
- `ews-calendar-latitude`
- `ews-calendar-longitude`

All of them are optional. Startup should succeed even if none are set.

## Runtime State

Local state such as bookmarks, package downloads, and autosave metadata is not tracked.

## Troubleshooting

- If a font is missing, Emacs falls back to its default font.
- If `ispell`, `aspell`, or `hunspell` is missing, spell-check setup is skipped.
- If the Org directories are unset or missing, the Org modules still load, but agenda and journal paths will be limited.
